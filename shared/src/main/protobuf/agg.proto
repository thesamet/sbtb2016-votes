syntax = "proto3";

package votes;

import "votes.proto";

message Aggregate {
    message Item {
        Vote.Language language = 1;
        int32 age = 2;
        int32 count =3;
    }
    repeated Item items = 1;
}
